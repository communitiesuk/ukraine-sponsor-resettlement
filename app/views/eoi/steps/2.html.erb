<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render partial: "shared/back_link" %>

    <%= form_for @application, url: "/expression-of-interest/steps/2", method: :post do |f| %>
      <%= f.govuk_text_field :email,
        label: { text: t('email.full', scope: "expression_of_interest.questions") },
        type: "email"
      %>
      <%= f.govuk_submit t('continue') %>
    <% end %>
  </div>
</div>

