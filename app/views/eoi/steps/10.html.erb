<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render partial: "shared/back_link" %>

    <%= form_for @application, url: "/expression-of-interest/steps/10", method: :post do |f| %>
      <%= f.govuk_collection_radio_buttons :family_type,
          @application.family_types.map{ |type| OpenStruct.new(id: type, name: t(type, scope: "expression_of_interest.family_type")) },
          :id,
          :name,
          legend: { text: t('family_type.full', scope: "expression_of_interest.questions") }
      %>
      <%= f.govuk_submit t('continue') %>
    <% end %>
  </div>
</div>
