<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds-from-desktop">

    <h1 class="govuk-heading-l">Check your answers before sending your application</h1>

    <h2 class="govuk-heading-m">Application details</h2>

    <dl class="govuk-summary-list govuk-!-margin-bottom-9">
      <div class="govuk-summary-list__row">
        <dt class="govuk-summary-list__key">
          Residential address
        </dt>
        <dd class="govuk-summary-list__value">
          <%= @application.residential_line_1 %>
          <br />

          <% if @application.residential_line_2.present? %>
            <%= @application.residential_line_2 %>
            <br />
          <% end %>

          <%= @application.residential_town %>
          <br />
          <%= @application.residential_postcode %>
        </dd>
        <dd class="govuk-summary-list__actions">
          <a class="govuk-link" href="/additional-info/steps/1">
            Change<span class="govuk-visually-hidden"> address</span>
          </a>
        </dd>
      </div>
    </dl>

    <dl class="govuk-summary-list govuk-!-margin-bottom-9">
      <div class="govuk-summary-list__row">
        <dt class="govuk-summary-list__key">
          <%= t('email.short', scope: "individual.questions") %>
        </dt>
        <dd class="govuk-summary-list__value">
          <% if @application.errors.include?(:email) %>
            <span class="govuk-error-message"><%= @application.errors[:email].join %></span>
          <% else %>
            <%= @application.email %>
          <% end %>
        </dd>
        <dd class="govuk-summary-list__actions">
          <a class="govuk-link" href="/additional-info/steps/2">
            Change<span class="govuk-visually-hidden"> email</span>
          </a>
        </dd>
      </div>
    </dl>

    <dl class="govuk-summary-list govuk-!-margin-bottom-9">
      <div class="govuk-summary-list__row">
        <dt class="govuk-summary-list__key">
          <%= t('phone_number.short', scope: "additional_info.questions") %>
        </dt>
        <dd class="govuk-summary-list__value">
          <% if @application.errors.include?(:email) %>
            <span class="govuk-error-message"><%= @application.errors[:phone_number].join %></span>
          <% else %>
            <%= @application.phone_number %>
          <% end %>
        </dd>
        <dd class="govuk-summary-list__actions">
          <a class="govuk-link" href="/additional-info/steps/3">
            Change<span class="govuk-visually-hidden"> phone number</span>
          </a>
        </dd>
      </div>
    </dl>

    <dl class="govuk-summary-list govuk-!-margin-bottom-9">
      <div class="govuk-summary-list__row">
        <dt class="govuk-summary-list__key">
          <%= t('proof_of_id.short', scope: "additional_info.questions") %>
        </dt>
        <dd class="govuk-summary-list__value">
          <% if @application.errors.include?(:proof_of_id) %>
            <span class="govuk-error-message"><%= @application.errors[:proof_of_id].join %></span>
          <% else %>
            <%= t(@application.proof_of_id, scope: "additional_info.proof_of_id") %>
          <% end %>
        </dd>
        <dd class="govuk-summary-list__actions">
          <a class="govuk-link" href="/additional-info/steps/4">
            Change<span class="govuk-visually-hidden"> proof of id</span>
          </a>
        </dd>
      </div>
    </dl>

    <h2 class="govuk-heading-m">Now send your application</h2>

    <p class="govuk-body">By submitting this application you are confirming that, to the best of your knowledge, the details you are providing are correct.</p>

    <%= form_for @application, url: "/additional-info/submit", method: :post do |f| %>
      <%= f.govuk_submit t('Accept and send') %>
    <% end %>
  </div>
</div>
