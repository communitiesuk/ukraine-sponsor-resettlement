<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render partial: "shared/back_link" %>

    <%= form_for @application, url: "/organisation/steps/1", method: :post do |f| %>
      <%= f.govuk_collection_radio_buttons :family_type,
          @application.family_types.map{ |type| OpenStruct.new(id: type, name: t(type, scope: "organisation.family_types")) },
          :id,
          :name,
          legend: { text: t('family_type.full', scope: "organisation.questions") }
      %>
      <%= f.govuk_submit t('continue') %>
    <% end %>
  </div>
</div>
